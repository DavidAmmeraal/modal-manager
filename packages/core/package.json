{
  "name": "@modal-manager/core",
  "version": "0.0.1",
  "module": "dist/esm/index.js",
  "main": "dist/cjs/index.js",
  "scripts": {
    "compile:cjs": "tsc -p ./tsconfig.build.json --module commonjs --outDir ./dist/cjs",
    "compile:es": "tsc -p ./tsconfig.build.json --module esnext --outDir ./dist/esm",
    "compile": "yarn run compile:cjs && yarn run compile:es",
    "lint": "eslint --ext .ts src"
  }
}
